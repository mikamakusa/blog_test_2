apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog_test.fullname" . }}
  labels:
    {{- include "blog_test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blog_test.selectorLabels" . | nindent 4 }}
